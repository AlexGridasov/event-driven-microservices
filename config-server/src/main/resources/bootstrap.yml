spring:
  cloud:
    config:
      server:
        native:
          search-locations:
           file:///${CONFIG_DIR_PATH}
    fail-fast: true
  security:
    user:
      name: spring_cloud_user
      password: 'Passw0rd'
